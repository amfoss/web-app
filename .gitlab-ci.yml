image: node:latest

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - npm install
    - npm run gitlab-build
    - rm -rf public
    - mv dist public

  artifacts:
    paths:
      - public # mandatory, other folder won't work
  only:
    - master
